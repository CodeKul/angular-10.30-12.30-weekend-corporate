import { MagicDivDirective } from './magic-div.directive';

describe('MagicDivDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicDivDirective();
    expect(directive).toBeTruthy();
  });
});
